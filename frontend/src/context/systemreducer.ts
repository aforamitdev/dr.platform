import { HeaderType, System } from '../components/types';

export const systemState: System = {
  currentHeaderIndex: HeaderType.SYSTEM,
};

export function reducer(state: System, action: any) {
  return state;
}
